#include "rendering/effects/Effect.hpp"

//-----------------------------------------------------------------------------

//void Effect::init(Array< Pass > && _passes)
//{
//	m_passes = std::move(_passes);
//}
//
////-----------------------------------------------------------------------------
//
//void Effect::Apply() const
//{
//	const int passesCount = m_passes.GetSize();
//	for (int i = 0; i < passesCount; ++i)
//		m_passes[i].Run();
//}

//-----------------------------------------------------------------------------
